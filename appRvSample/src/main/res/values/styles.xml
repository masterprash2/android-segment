<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="DefaultTabText" parent="TextAppearance.AppCompat.Button">
        <item name="android:textSize">15sp</item>
        <item name="android:textColor">#988677</item>
        <item name="android:textStyle">normal</item>
    </style>

    <style name="DefaultTabTitleStyle" parent="Widget.Design.TabLayout">
        <item name="tabTextAppearance">@style/DefaultTabText</item>
        <item name="tabSelectedTextColor">#666666</item>
        <item name="tabIndicatorColor">#000000</item>
        <item name="tabIndicatorHeight">4dp</item>
    </style>



</resources>
